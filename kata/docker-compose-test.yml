version: "2.1"
services:
    postgres:
        container_name: paladin-test-db
        image: postgres
        ports:
            - 5432:5432
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: my-secret
