suite: Snapshot tests
tests:
  - it: Basic snapshot test
    asserts:
      - matchSnapshot: {}
    set:
      unittest: true
  - it: Mode none
    asserts:
      - matchSnapshot: {}
    set:
      unittest: true
      mode: none
  - it: Mode basenet
    asserts:
      - matchSnapshot: {}
    set:
      unittest: true
      mode: basenet
  - it: Mode devnet
    asserts:
      - matchSnapshot: {}
    set:
      unittest: true
      mode: devnet

      
